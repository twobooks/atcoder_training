import math
import os

import numpy as np
import scipy as scp
import sklearn as skl

# 入力の受け取り
n = int(input())
n,k = map(n,r = map(int,input().split())

arr = np.array(input().split(),dtype=np.int64)

# 演算子
a + b         # 加算
a - b         # 減算
a * b         # 乗算
a / b         # 除算
a % b         # a を b で割った余り
a ** b        # a の b 乗
a // b        # 切り捨て除算

# コンソールのクリア
os.sysytem("cls")

